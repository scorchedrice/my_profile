import { motion } from "framer-motion"

export default function IntroPage() {
  return (
    <motion.div className="h-[100dvh] bg-purple-300 flex items-center justify-center" id="Intro">
      Intro
    </motion.div>
  )
}